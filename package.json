{
  "name": "jasons-formal",
  "version": "0.0.1",
  "description": "",
  "engines": {},
  "main": "",
  "scripts": {
    "build-dev": "cp -n ./config/env/example.development.js ./config/env/development.js && webpack --progress --colors --watch",
    "build": "NODE_ENV=production webpack -p ",
    "run": "BLUEBIRD_WARNINGS=0 sails lift",
    "test-coverage": "BLUEBIRD_WARNINGS=0 NODE_ENV=test nyc --reporter=html --reporter=text  _mocha -- --recursive test --compilers js:babel-core/register --timeout 45000",
    "test-mocha": "BLUEBIRD_WARNINGS=0 NODE_ENV=test  _mocha --recursive test --compilers js:babel-core/register --timeout 45000",
    "test": "BLUEBIRD_WARNINGS=0 NODE_ENV=test nyc  --reporter=html --reporter=text _mocha --recursive test --compilers js:babel-core/register --timeout 45000",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/joshgagnon/jasons-formal.git"
  },
  "author": "",
  "devDependencies": {},
  "dependencies": {
    "bluebird": "^3.5.1",
    "express": "^4.16.2",
    "json-schemer": "github:joshgagnon/json-schemer",
    "react": "^16.2.0",
    "redux": "^3.7.2",
    "webpack": "^3.10.0"
  }
}
